name: ci

on:
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: prefix-dev/setup-pixi@v0.8.10
        with:
          pixi-version: v0.44.0
          cache: true

      - run: pixi run pytest
